name: validate-on-pull-request

on:
  pull_request:
    branches: [ "develop", "main" ]
    types: [opened, reopened, synchronize]

env:
  MAVEN_OPTS: -Djava.security.egd=file:/dev/./urandom

jobs:
  verify:
    name: Verify PR
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21
          cache: maven

      - name: Cache SonarCloud packages
        uses: actions/cache@v4
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

      # -Ppublish builds the source and javadoc jars too, so a PR cannot
      # merge with javadoc errors that would break publishing on push.
      # sonar:sonar runs after verify so the JaCoCo XML report is in place;
      # SonarCloud org/host/projectKey come from the pom.
      - name: Build, test, enforce coverage and analyze with SonarCloud
        run: >
          ./mvnw --batch-mode -Ppublish clean verify sonar:sonar
          -Dsonar.working.directory=.scannerwork
          -Dsonar.pullrequest.key=${{ github.event.pull_request.number }}
          -Dsonar.pullrequest.branch=${{ github.head_ref }}
          -Dsonar.pullrequest.base=${{ github.base_ref }}
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: jacoco-report
          path: target/site/jacoco/
          retention-days: 14

      - name: SonarCloud Quality Gate Check
        uses: sonarsource/sonarqube-quality-gate-action@master
        timeout-minutes: 5
        with:
          projectBaseDir: .
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
